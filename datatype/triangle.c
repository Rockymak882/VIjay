/* wap to find triangle */
#include <stdio.h>
void main()
{
    float b,h;
    printf("Enter Base and Height: ");
    scanf("%f %f",&b,&h);
   
    float area=(b*h)/2;
 
    
    printf("this is the area of Triangle  %0.2f",area);
}