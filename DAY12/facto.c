/* wap to find an factorial */
#include<stdio.h>
#include<conio.h>

void main()
{

    int i=1,n,fact=1,facto;
    printf("enter fact number = ",n);
    scanf("%d",&n);

    while(i<=n)
    {
        facto=fact*n,n++;
       
    }
   printf("%d",facto);
}